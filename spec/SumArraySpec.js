describe("SumArray", function() {
  
  it("should sum the elements of the array", function() {
    expect(sumArray()).toEqual(15); 
  });
});

describe("AverageArray", function() {
  it("should show the average of the array", function() {
    expect(averageArray()).toEqual(3);
  });
});
